<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. Compose handles the actual Material3 theming.
         This XML theme is only used during app launch (splash) before Compose takes over. -->
    <style name="Theme.MeseroApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
